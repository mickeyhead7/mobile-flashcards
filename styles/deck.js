import { StyleSheet } from 'react-native';

/**
 * @description Deck styles
 */
const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        margin: 20,
    },
    label: {
        fontSize: 20,
        fontWeight: 'bold',
        marginBottom: 20,
        textAlign: 'center',
    },
    subLabel: {
        marginBottom: 20,
        textAlign: 'center',
    },
    button: {
        marginBottom: 20,
    },
});

export default styles;
